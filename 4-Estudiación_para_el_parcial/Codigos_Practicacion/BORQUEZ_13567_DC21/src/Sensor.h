#include <string>
class Sensor{
    public:
        void agregar()
    private:
        std::string magnitud;       // El nombre de la magnitud medida
        std::string unidadMedida;   // El nombre de la unidad en que se mide
};